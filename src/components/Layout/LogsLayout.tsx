import React from "react";
import Layout from "./Layout";

const Home: React.FC = () => {
    return (
        <Layout>
            <h1>ログを出力</h1>
        </Layout>
    );
};

export default Home;
