import React from "react";
import Layout from "./Layoutcomponents/Layout";

const Home: React.FC = () => {
    return (
        <Layout>
            <h1>ログを出力</h1>
        </Layout>
    );
};

export default Home;
